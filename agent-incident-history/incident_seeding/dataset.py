from __future__ import annotations

import logging
from datetime import datetime, timedelta, timezone
from typing import Any

from incident_seeding.models import SeedIncident

logger = logging.getLogger("incident-history-seed")

# ---------------------------------------------------------------------------
# High-Quality Curated DevSecOps Incident Dataset (50-80 Distinct Incidents)
# ---------------------------------------------------------------------------

CURATED_INCIDENT_SPECS: list[dict[str, Any]] = [
    # ── Docker & Container Security ──────────────────────────────────────────
    {
        "incident_id": "INC-2001",
        "title": "Container Privilege Escalation via USER root in Dockerfile",
        "category": "docker",
        "severity": "critical",
        "service": "api-gateway",
        "affected_services": ["api-gateway", "auth-service"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 75,
        "summary": "An application container deployed with USER root was compromised via RCE, allowing full container filesystem overwrite.",
        "description": "The Dockerfile explicitly set 'USER root'. A deserialization vulnerability in the API gateway enabled remote code execution, giving the attacker root privileges within the container.",
        "root_cause": "Dockerfile misconfiguration executing processes as UID 0 instead of an unprivileged service user.",
        "impact": "Full container compromise, exfiltration of local environment secrets, and 75 minutes of service downtime.",
        "resolution": "Rebuilt image with dedicated 'appuser' (UID 10001), dropped default capabilities, and rolled back deployment.",
        "timeline": "10:00 - Deployment triggered; 10:15 - Anomaly detected; 10:35 - Root compromise confirmed; 11:15 - Rollback complete.",
        "lessons_learned": "Never run container workloads as root. Enforce container user linter rules in CI.",
        "preventive_controls": ["Conftest Dockerfile linting", "Kube-linter non-root policy", "ReadOnlyRootFilesystem enforcement"],
        "tags": ["docker", "root", "privilege-escalation", "rce", "container"],
        "outcome": "Rollback",
        "days_ago": 12,
    },
    {
        "incident_id": "INC-2002",
        "title": "Host Node Escape via Docker Socket Bind Mount",
        "category": "docker",
        "severity": "critical",
        "service": "ci-runner-agent",
        "affected_services": ["ci-runner-agent", "k8s-node-worker-03"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 120,
        "summary": "Bind-mounting /var/run/docker.sock into a container enabled full host node compromise.",
        "description": "A background worker container mounted docker.sock for sibling container execution. A command injection vulnerability in the worker allowed an attacker to issue API commands directly to the host Docker daemon.",
        "root_cause": "docker.sock bind mount provided unrestricted control over the host Docker daemon.",
        "impact": "Host node compromise, unauthorized SSH key placement on host, and 2-hour cluster node isolation.",
        "resolution": "Terminated compromised worker node, removed docker.sock volume mount, and migrated to Kaniko for rootless builds.",
        "timeline": "14:00 - Malicious container API command; 14:20 - Host access detected by Falco; 16:00 - Node re-imaged.",
        "lessons_learned": "Mounting docker.sock is equivalent to granting root access to the underlying host node.",
        "preventive_controls": ["OPA Gatekeeper docker.sock restriction", "Rootless build tools (Kaniko)", "Falco runtime monitoring"],
        "tags": ["docker", "docker-socket", "container-escape", "host-compromise", "privilege-escalation"],
        "outcome": "Security Incident",
        "days_ago": 25,
    },
    {
        "incident_id": "INC-2003",
        "title": "Host Network Namespace Interception via --network=host",
        "category": "docker",
        "severity": "high",
        "service": "metrics-exporter",
        "affected_services": ["metrics-exporter", "redis-cache"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 45,
        "summary": "Deploying container with --network=host exposed host loopback services to container traffic.",
        "description": "The metrics-exporter container was started with host networking enabled. A vulnerability in the exporter allowed an attacker to intercept unencrypted localhost traffic bound for a local Redis instance.",
        "root_cause": "Use of --network=host disabled network namespace isolation between container and host.",
        "impact": "Interception of internal cache tokens and 45-minute metrics service degradation.",
        "resolution": "Disabled host networking, attached container to bridge network, and flushed Redis sessions.",
        "timeline": "08:10 - Deployment active; 08:25 - Redis connection anomaly; 08:55 - Re-deployed with isolated network.",
        "lessons_learned": "Host networking eliminates network isolation and exposes local bind interfaces.",
        "preventive_controls": ["Docker compose network isolation linting", "Cilium network policy"],
        "tags": ["docker", "host-network", "networking", "information-disclosure", "container"],
        "outcome": "Hotfix",
        "days_ago": 38,
    },
    {
        "incident_id": "INC-2004",
        "title": "Unpinned Image Tag Supply Chain Poisoning (FROM image:latest)",
        "category": "docker",
        "severity": "high",
        "service": "payment-processor",
        "affected_services": ["payment-processor"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 90,
        "summary": "Using 'FROM node:latest' pulled a compromised upstream base image containing malicious telemetry code.",
        "description": "The payment processor Dockerfile referenced the 'latest' tag without SHA digest pinning. An upstream maintainer account compromise resulted in a malicious layer being pulled automatically during deployment.",
        "root_cause": "Omission of immutable SHA256 digest pinning for base container images.",
        "impact": "Malicious outbound HTTP traffic from payment service and 90-minute deployment freeze.",
        "resolution": "Pinned base image to trusted SHA256 digest, revoked compromised API keys, and redeployed.",
        "timeline": "16:00 - Auto-build pulled tainted latest tag; 16:30 - Egress anomaly flagged; 17:30 - SHA pinned image deployed.",
        "lessons_learned": "Mutable tags (latest, v1) can change without notice. Always pin base images by SHA256 digest.",
        "preventive_controls": ["Trivy vulnerability scan blocking", "SHA256 digest pinning policy"],
        "tags": ["docker", "latest-tag", "supply-chain", "unpinned-dependency", "container"],
        "outcome": "Rollback",
        "days_ago": 45,
    },
    {
        "incident_id": "INC-2005",
        "title": "Hardcoded AWS Credentials in Dockerfile ENV Instruction",
        "category": "docker",
        "severity": "critical",
        "service": "batch-importer",
        "affected_services": ["batch-importer", "aws-s3"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 60,
        "summary": "AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY hardcoded in Dockerfile ENV layer were leaked via public image registry.",
        "description": "A developer committed a Dockerfile containing 'ENV AWS_SECRET_ACCESS_KEY=...' for local testing. The image was pushed to a public ECR repository, and GitHub secret scanners flagged immediate external access.",
        "root_cause": "Hardcoded secrets in Dockerfile build instructions exposed in layer metadata.",
        "impact": "Compromise of AWS IAM credentials, requiring emergency key revocation and audit of S3 access logs.",
        "resolution": "Revoked AWS key pair, deleted public ECR image tag, and migrated to AWS IAM Roles for Service Accounts (IRSA).",
        "timeline": "11:00 - Git commit pushed; 11:05 - ECR image published; 11:12 - Automated key revocation triggered.",
        "lessons_learned": "Dockerfile ENV and ARG values persist in image layer history even if removed in later stages.",
        "preventive_controls": ["Trufflehog git pre-commit hook", "AWS IAM IRSA integration", "Public registry push blocking"],
        "tags": ["docker", "secrets-leak", "aws-credentials", "env-variables", "information-disclosure"],
        "outcome": "Security Incident",
        "days_ago": 52,
    },
    {
        "incident_id": "INC-2006",
        "title": "Remote File Fetch via ADD Instruction in Dockerfile",
        "category": "docker",
        "severity": "medium",
        "service": "document-renderer",
        "affected_services": ["document-renderer"],
        "environment": "staging",
        "rollback": True,
        "duration_minutes": 30,
        "summary": "Using 'ADD https://...' in Dockerfile auto-extracted an unverified tarball containing a web shell.",
        "description": "The Dockerfile used 'ADD https://external-cdn.com/asset.tar.gz /app/' which auto-extracted a compromised tarball during build execution.",
        "root_cause": "Use of ADD with remote URL instead of curl + checksum verification + COPY.",
        "impact": "Tainted container image built in CI pipeline.",
        "resolution": "Replaced ADD with 'curl -fsSL ... | sha256sum' verification and COPY.",
        "timeline": "09:00 - CI build completed; 09:15 - Image scan failed; 09:30 - Dockerfile corrected.",
        "lessons_learned": "Prefer COPY over ADD; fetch remote resources explicitly with checksum validation.",
        "preventive_controls": ["Hadolint DL3020 rule enforcement", "Container image vulnerability scanning"],
        "tags": ["docker", "add-instruction", "remote-url", "unverified-download"],
        "outcome": "Hotfix",
        "days_ago": 60,
    },

    # ── Docker Compose Security ───────────────────────────────────────────────
    {
        "incident_id": "INC-2010",
        "title": "Privileged Execution in Docker Compose Service Definition",
        "category": "docker-compose",
        "severity": "critical",
        "service": "background-worker",
        "affected_services": ["background-worker", "host-kernel"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 105,
        "summary": "Setting 'privileged: true' in docker-compose.yml enabled container breakout to host device node.",
        "description": "A docker-compose.yml configuration included 'privileged: true' to allow raw socket creation. An RCE in the background worker allowed the attacker to access host /dev/sda1 directly.",
        "root_cause": "Overly permissive 'privileged: true' flag in production Compose file.",
        "impact": "Host disk access attempt, emergency container isolation, and 105-minute worker downtime.",
        "resolution": "Removed 'privileged: true', granted only CAP_NET_RAW capability, and updated Compose deployment scripts.",
        "timeline": "13:00 - Compose stack deployed; 13:40 - Kernel alert on raw disk access; 14:45 - Patched stack deployed.",
        "lessons_learned": "privileged: true disables all Linux security barriers. Use targeted cap_add instead.",
        "preventive_controls": ["Docker Compose security linter", "Linux Capability scoping"],
        "tags": ["docker-compose", "privileged", "container-escape", "host-compromise"],
        "outcome": "Security Incident",
        "days_ago": 15,
    },
    {
        "incident_id": "INC-2011",
        "title": "Docker Socket Mount in Compose Multi-Container Stack",
        "category": "docker-compose",
        "severity": "critical",
        "service": "management-dashboard",
        "affected_services": ["management-dashboard", "docker-engine"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 80,
        "summary": "Dashboard service mounted /var/run/docker.sock, enabling unauthorized container execution on host.",
        "description": "The admin dashboard service mounted the host Docker socket to list running containers. XSS in the dashboard allowed an adversary to trigger arbitrary container runs with host root mounts.",
        "root_cause": "Exposing host Docker socket to web-facing management UI container.",
        "impact": "Unauthorized container creation on production host, requiring full host cleanup and credential rotation.",
        "resolution": "Replaced Docker socket mount with a read-only Docker HTTP proxy filtering dangerous endpoints.",
        "timeline": "15:10 - XSS triggered container create call; 15:30 - Rogue container detected; 16:30 - Socket proxy deployed.",
        "lessons_learned": "Never expose the raw Docker socket to web applications. Use a scoped API proxy.",
        "preventive_controls": ["Socket proxy wrapper (Tecnativa)", "Docker socket mount audit"],
        "tags": ["docker-compose", "docker-socket", "xss", "container-escape"],
        "outcome": "Rollback",
        "days_ago": 28,
    },

    # ── Kubernetes Security ──────────────────────────────────────────────────
    {
        "incident_id": "INC-2020",
        "title": "Privileged Pod SecurityContext Breakout in Production Kubernetes",
        "category": "kubernetes",
        "severity": "critical",
        "service": "storage-node-agent",
        "affected_services": ["storage-node-agent", "k8s-worker-node-07"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 110,
        "summary": "Setting 'securityContext.privileged: true' in Deployment manifest allowed worker node root takeover.",
        "description": "A daemonset pod manifest contained 'privileged: true'. A vulnerable dependency allowed remote code execution, giving the attacker root access to the node's /proc and /sys filesystems.",
        "root_cause": "Privileged security context enabled on Kubernetes pod manifest.",
        "impact": "Node compromise, potential cross-pod data leakage, and 110-minute node drain and re-image.",
        "resolution": "Enforced Kyverno Pod Security Standard (Baseline/Restricted), removed privileged flag, and rotated node SA tokens.",
        "timeline": "02:00 - Pod deployed; 02:35 - Malicious cgroup modification detected; 03:50 - Kyverno policy deployed.",
        "lessons_learned": "PodSecurityStandards must block privileged pods across all namespaces.",
        "preventive_controls": ["Kyverno / OPA Gatekeeper PSS Restricted enforcement", "Kube-bench CIS benchmarks"],
        "tags": ["kubernetes", "privileged-pod", "container-escape", "node-compromise", "security-context"],
        "outcome": "Security Incident",
        "days_ago": 8,
    },
    {
        "incident_id": "INC-2021",
        "title": "HostPath Volume Mount Host Filesystem Overwrite",
        "category": "kubernetes",
        "severity": "critical",
        "service": "log-collector-daemon",
        "affected_services": ["log-collector-daemon", "k8s-node-etc"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 95,
        "summary": "Mounting hostPath '/' into a pod allowed unauthorized modification of host system configuration.",
        "description": "A logging pod mounted hostPath path: '/' with write permissions. Path traversal in the logging daemon allowed writing files into host /etc/cron.d.",
        "root_cause": "Overly broad hostPath volume mount (path: '/') without readOnly: true.",
        "impact": "Host cron persistence creation attempt, node quarantine, and 95-minute daemonset rollout delay.",
        "resolution": "Restricted hostPath to specific directory (/var/log), set readOnly: true, and enforced Gatekeeper hostPath policy.",
        "timeline": "11:00 - Log daemon updated; 11:20 - File creation alert on host /etc; 12:35 - Restricted pod deployed.",
        "lessons_learned": "hostPath mounts should be avoided or restricted to specific subdirectories with read-only flags.",
        "preventive_controls": ["OPA Gatekeeper hostPath whitelist", "ReadOnlyRootFilesystem policy"],
        "tags": ["kubernetes", "hostpath", "volume-mount", "privilege-escalation", "file-overwrite"],
        "outcome": "Rollback",
        "days_ago": 22,
    },
    {
        "incident_id": "INC-2022",
        "title": "HostNetwork and HostPID Namespace Sharing Exploitation",
        "category": "kubernetes",
        "severity": "high",
        "service": "network-monitor",
        "affected_services": ["network-monitor", "kubelet-api"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 65,
        "summary": "Enabling hostNetwork: true and hostPID: true exposed host processes and Kubelet traffic to container.",
        "description": "A monitoring pod set hostNetwork: true and hostPID: true. A compromised process in the container inspected host process environment variables and captured plain-text secrets.",
        "root_cause": "Unnecessary sharing of host network and PID namespaces in pod manifest.",
        "impact": "Exposure of host environment variables and 65-minute pod isolation.",
        "resolution": "Disabled hostNetwork and hostPID, migrated to Kubernetes CNI eBPF monitoring, and rotated exposed secrets.",
        "timeline": "09:15 - Monitoring pod deployed; 09:40 - Process inspection detected; 10:20 - Pod spec patched.",
        "lessons_learned": "hostNetwork and hostPID break process and network isolation boundary between pod and host.",
        "preventive_controls": ["PodSecurityAdmission restricted profile", "NetworkPolicy isolation"],
        "tags": ["kubernetes", "hostnetwork", "hostpid", "information-disclosure", "namespace"],
        "outcome": "Hotfix",
        "days_ago": 34,
    },
    {
        "incident_id": "INC-2023",
        "title": "Node Resource Exhaustion (OOM) via Missing Pod Resource Limits",
        "category": "kubernetes",
        "severity": "high",
        "service": "data-processor-worker",
        "affected_services": ["data-processor-worker", "shared-node-02", "auth-api"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 50,
        "summary": "Deployment without CPU/memory limits consumed all node memory, causing OOM-killer evictions of adjacent pods.",
        "description": "A batch processing deployment omitted resources.limits. Memory leak in the batch job consumed 64GB node RAM, triggering kernel OOM-killer which terminated the co-located Auth API pod.",
        "root_cause": "Missing CPU and Memory resource limits in Kubernetes container specification.",
        "impact": "Outage of co-located Auth API pod on shared node and 50 minutes of authentication failures.",
        "resolution": "Enforced LimitRanges in namespace, added explicit resource requests and limits to deployment spec.",
        "timeline": "14:00 - Batch job started; 14:30 - Auth API pod OOMKilled; 14:50 - Resource limits applied and pod restarted.",
        "lessons_learned": "Every container spec must define CPU and memory requests and limits to guarantee QoS.",
        "preventive_controls": ["Kubernetes LimitRange policy", "Kube-linter missing-resources rule"],
        "tags": ["kubernetes", "resource-limits", "oom-kill", "denial-of-service", "node-starvation"],
        "outcome": "Production Outage",
        "days_ago": 40,
    },
    {
        "incident_id": "INC-2024",
        "title": "Kubernetes Service Account Secret Token Auto-Mount Abuse",
        "category": "kubernetes",
        "severity": "high",
        "service": "frontend-web",
        "affected_services": ["frontend-web", "kubernetes-apiserver"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 60,
        "summary": "Default automountServiceAccountToken: true exposed K8s API token to public-facing frontend pod.",
        "description": "The public frontend pod automatically mounted the service account token. An SSRF vulnerability allowed reading /var/run/secrets/kubernetes.io/serviceaccount/token and querying the K8s API server.",
        "root_cause": "Failure to set automountServiceAccountToken: false on pods not interacting with K8s API.",
        "impact": "Reconnaissance against Kubernetes API server using default service account token.",
        "resolution": "Set automountServiceAccountToken: false on all web workloads and applied RBAC restrictions.",
        "timeline": "16:00 - SSRF exploited; 16:20 - K8s API audit log flagged unauthorized call; 17:00 - Automount disabled.",
        "lessons_learned": "Disable service account token auto-mounting unless the workload explicitly requires K8s API access.",
        "preventive_controls": ["automountServiceAccountToken: false by default", "RBAC least privilege policy"],
        "tags": ["kubernetes", "service-account", "token-leak", "ssrf", "rbac"],
        "outcome": "Hotfix",
        "days_ago": 55,
    },

    # ── Terraform & Infrastructure as Code ───────────────────────────────────
    {
        "incident_id": "INC-2030",
        "title": "Public S3 Bucket Exposure via Public-Read ACL in Terraform",
        "category": "terraform",
        "severity": "critical",
        "service": "customer-exports-storage",
        "affected_services": ["customer-exports-storage", "aws-s3"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 90,
        "summary": "Terraform configuration set acl = 'public-read' on an S3 bucket containing customer PDF exports.",
        "description": "A Terraform PR modified an S3 bucket resource by setting `acl = 'public-read'` and `block_public_acls = false`. Automated internet scanners discovered and indexed the bucket URL.",
        "root_cause": "Terraform IaC misconfiguration enabling public read access on private data storage.",
        "impact": "Exposure of 15,000 customer export documents, regulatory notification, and 90-minute remediation.",
        "resolution": "Reverted ACL to 'private', enabled AWS S3 Block Public Access at account level, and applied bucket policy.",
        "timeline": "08:00 - Terraform applied; 08:45 - Public access alert from AWS Security Hub; 09:30 - Bucket secured.",
        "lessons_learned": "Enforce AWS Account-level S3 Block Public Access and block 'public-read' ACLs in CI checkov scans.",
        "preventive_controls": ["AWS S3 Block Public Access", "Checkov CKV_AWS_20 rule", "Terraform TFLint security rules"],
        "tags": ["terraform", "s3", "public-bucket", "data-exposure", "aws", "iac"],
        "outcome": "Security Incident",
        "days_ago": 5,
    },
    {
        "incident_id": "INC-2031",
        "title": "Wildcard IAM Admin Policy Attachment via Terraform",
        "category": "terraform",
        "severity": "critical",
        "service": "lambda-processor",
        "affected_services": ["lambda-processor", "aws-iam"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 100,
        "summary": "Terraform IAM policy assigned Action: '*' and Resource: '*' to a serverless processing role.",
        "description": "An IAM role created in Terraform attached a policy statement with `Action: '*'` and `Resource: '*'`. An RCE in the Lambda function allowed full control over the AWS account.",
        "root_cause": "Wildcard IAM permissions in Terraform module violating least-privilege principle.",
        "impact": "Full AWS account administrator access granted to Lambda role, emergency IAM lockdown.",
        "resolution": "Replaced wildcard policy with specific S3 and DynamoDB resource permissions, rotated role credentials.",
        "timeline": "10:00 - Terraform applied; 10:40 - IAM privilege escalation alert; 11:40 - Scoped policy deployed.",
        "lessons_learned": "Never use Action: '*' or Resource: '*' in application IAM roles. Enforce IAM Access Analyzer.",
        "preventive_controls": ["AWS IAM Access Analyzer", "Checkov CKV_AWS_1", "tfsec IAM wildcard policy check"],
        "tags": ["terraform", "iam", "wildcard-policy", "privilege-escalation", "aws"],
        "outcome": "Security Incident",
        "days_ago": 18,
    },
    {
        "incident_id": "INC-2032",
        "title": "Open SSH / Database Ingress (0.0.0.0/0) in Security Group",
        "category": "terraform",
        "severity": "critical",
        "service": "db-bastion-host",
        "affected_services": ["db-bastion-host", "postgres-rds"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 85,
        "summary": "Terraform security group rule opened port 22 (SSH) and 5432 (PostgreSQL) to 0.0.0.0/0.",
        "description": "A developer opened port 22 and 5432 to `cidr_blocks = ['0.0.0.0/0']` in a Terraform security group rule for staging testing. The rule was merged into production.",
        "root_cause": "Permissive 0.0.0.0/0 ingress rule added to Terraform security group resource.",
        "impact": "Brute-force SSH attempts detected from external IPs, 85-minute network isolation.",
        "resolution": "Restricted SSH and DB ingress to internal VPN CIDR block only, enabled AWS GuardDuty SSH alert.",
        "timeline": "13:00 - Terraform applied; 13:25 - GuardDuty SSH brute force finding; 14:25 - CIDR restricted.",
        "lessons_learned": "0.0.0.0/0 ingress should never be allowed for SSH, RDP, or database ports.",
        "preventive_controls": ["Checkov CKV_AWS_24 rule", "tfsec open ingress check", "AWS Config Security Group rule"],
        "tags": ["terraform", "security-group", "open-ingress", "ssh", "database", "0.0.0.0/0"],
        "outcome": "Rollback",
        "days_ago": 30,
    },
    {
        "incident_id": "INC-2033",
        "title": "Unencrypted Production RDS Database Instance in Terraform",
        "category": "terraform",
        "severity": "high",
        "service": "user-db-cluster",
        "affected_services": ["user-db-cluster", "aws-rds"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 120,
        "summary": "Terraform RDS resource created with storage_encrypted = false in production.",
        "description": "A new PostgreSQL RDS instance was provisioned with `storage_encrypted = false` due to a missing KMS key argument in the Terraform module.",
        "root_cause": "Defaulting storage_encrypted to false in Terraform RDS module.",
        "impact": "Non-compliant unencrypted database storage containing PII data, requiring snapshot and re-encryption migration.",
        "resolution": "Created encrypted snapshot, restored to new encrypted RDS instance, and updated Terraform module default.",
        "timeline": "09:00 - Database created; 10:00 - Compliance audit failure; 11:00 - Encrypted cluster cutover complete.",
        "lessons_learned": "Mandate storage_encrypted = true and KMS customer managed keys for all databases.",
        "preventive_controls": ["Checkov CKV_AWS_16 rule", "AWS Config rds-storage-encrypted"],
        "tags": ["terraform", "rds", "unencrypted-storage", "database", "compliance"],
        "outcome": "Hotfix",
        "days_ago": 48,
    },
    {
        "incident_id": "INC-2034",
        "title": "Publicly Accessible RDS Instance in Public Subnet",
        "category": "terraform",
        "severity": "critical",
        "service": "analytics-db",
        "affected_services": ["analytics-db", "aws-rds"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 70,
        "summary": "Terraform set publicly_accessible = true on RDS instance associated with public subnets.",
        "description": "The `aws_db_instance` resource set `publicly_accessible = true` and was placed in a public subnet, exposing the database endpoint on a public IP address.",
        "root_cause": "publicly_accessible = true combined with public subnet placement in Terraform.",
        "impact": "Database publicly reachable on TCP 5432, 70-minute emergency migration to private subnets.",
        "resolution": "Set `publicly_accessible = false`, moved DB subnets to private VPC subnets, and rotated DB passwords.",
        "timeline": "15:00 - DB deployed; 15:20 - Automated port scan alert; 16:10 - Private subnet migration complete.",
        "lessons_learned": "Production databases must always reside in private subnets with publicly_accessible = false.",
        "preventive_controls": ["Checkov CKV_AWS_17", "tfsec rds-publicly-accessible check"],
        "tags": ["terraform", "rds", "public-db", "networking", "aws"],
        "outcome": "Security Incident",
        "days_ago": 62,
    },

    # ── GitHub Actions & CI/CD Security ──────────────────────────────────────
    {
        "incident_id": "INC-2040",
        "title": "GitHub Token Exfiltration via Overly Permissive permissions: write-all",
        "category": "github-actions",
        "severity": "critical",
        "service": "github-ci-pipeline",
        "affected_services": ["github-ci-pipeline", "github-repo"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 60,
        "summary": "Setting 'permissions: write-all' in workflow enabled a compromised step to push commits directly to main.",
        "description": "A workflow set `permissions: write-all`. A third-party dependency in a build step exfiltrated the `GITHUB_TOKEN` and created an unauthorized commit on the main branch.",
        "root_cause": "Top-level `permissions: write-all` granting excessive GitHub API scope to workflow jobs.",
        "impact": "Unauthorized commit to main branch, branch protection bypass, and 1-hour repository freeze.",
        "resolution": "Reverted unauthorized commit, replaced write-all with granular minimal permissions (`contents: read`), and rotated tokens.",
        "timeline": "14:00 - Workflow executed; 14:10 - Unexpected push to main; 15:00 - Scoped permissions enforced.",
        "lessons_learned": "Enforce least-privilege permissions in GitHub Actions. Default to `permissions: {}` or `contents: read`.",
        "preventive_controls": ["zizmor GHA security linter", "Step-security Harden-Runner", "GitHub Repo default permissions: read"],
        "tags": ["github-actions", "permissions", "write-all", "token-exfiltration", "ci-cd"],
        "outcome": "Security Incident",
        "days_ago": 10,
    },
    {
        "incident_id": "INC-2041",
        "title": "Plaintext Secrets Echoed to GitHub Actions Build Logs",
        "category": "github-actions",
        "severity": "critical",
        "service": "deploy-workflow",
        "affected_services": ["deploy-workflow", "actions-logs"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 40,
        "summary": "Workflow run step executed 'echo ${{ secrets.DEPLOY_KEY }}' exposing production deployment key in public logs.",
        "description": "A debugging step added `run: echo \"Key: ${{ secrets.PROD_SSH_KEY }}\"` to a workflow. The SSH private key was printed into the Actions log output.",
        "root_cause": "Direct use of echo on secret variables in shell run steps.",
        "impact": "Exposure of production SSH key in Actions log archive, requiring key revocation across all servers.",
        "resolution": "Deleted workflow run log history, revoked and regenerated SSH key pair, and added pre-commit GHA secret echo linter.",
        "timeline": "10:00 - Workflow ran; 10:15 - Key in logs noticed by dev; 10:40 - SSH key rotated on all servers.",
        "lessons_learned": "Never echo secret expressions. Pass secrets via environment variables or secret-masking wrappers.",
        "preventive_controls": ["Action-lint secret-echo check", "Trufflehog log scanner"],
        "tags": ["github-actions", "secrets-echo", "secret-leak", "log-exposure"],
        "outcome": "Security Incident",
        "days_ago": 20,
    },
    {
        "incident_id": "INC-2042",
        "title": "Supply Chain Attack via Unpinned GitHub Action (uses: action@main)",
        "category": "github-actions",
        "severity": "high",
        "service": "release-pipeline",
        "affected_services": ["release-pipeline"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 85,
        "summary": "Workflow referenced 'uses: third-party/action@main' which pulled a malicious update from a hijacked repo.",
        "description": "The release workflow referenced a third-party action pinned to `@main`. The maintainer's account was hijacked, and a malicious commit was pushed to main that attempted to steal AWS credentials.",
        "root_cause": "Referencing mutable git branch (@main) instead of an immutable 40-character commit SHA.",
        "impact": "Malicious code execution during build pipeline, requiring credential rotation for CI runners.",
        "resolution": "Pinned all action references to full commit SHAs (`uses: action@b4ffde65f...`) and enabled Dependabot SHA pinning.",
        "timeline": "11:00 - Action maintainer compromised; 11:30 - CI run executed payload; 12:55 - SHA pinning applied across repo.",
        "lessons_learned": "Mutable tags (@v1, @main) can be updated silently. Always pin GitHub Actions to full commit SHAs.",
        "preventive_controls": ["pin-github-action CLI", "zizmor unpinned-uses rule", "Dependabot version updates"],
        "tags": ["github-actions", "unpinned-action", "supply-chain", "third-party", "ci-cd"],
        "outcome": "Rollback",
        "days_ago": 32,
    },
    {
        "incident_id": "INC-2043",
        "title": "PwnRequest Exploitation via pull_request_target Trigger",
        "category": "github-actions",
        "severity": "critical",
        "service": "pull-request-checker",
        "affected_services": ["pull-request-checker", "github-secrets"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 90,
        "summary": "Workflow used 'on: pull_request_target' and checked out untrusted head code, leaking repository secrets.",
        "description": "A workflow used `pull_request_target` (which runs in target context with access to secrets) and checked out code from the PR head using `actions/checkout` with `ref: ${{ github.event.pull_request.head.sha }}`. A fork PR submitted malicious Makefile code.",
        "root_cause": "Combining pull_request_target trigger with checkout of untrusted pull request head code.",
        "impact": "Exfiltration of repository secrets by external fork contributor.",
        "resolution": "Switched trigger to `pull_request` for untrusted code execution and separated secret-requiring jobs.",
        "timeline": "16:00 - Fork PR submitted; 16:05 - Exfiltration script ran; 17:35 - Secrets rotated, workflow rewritten.",
        "lessons_learned": "Never check out untrusted PR head code in a pull_request_target workflow.",
        "preventive_controls": ["zizmor pull_request_target rule", "CodeQL GHA security analysis"],
        "tags": ["github-actions", "pull-request-target", "pwnrequest", "secret-leak", "supply-chain"],
        "outcome": "Security Incident",
        "days_ago": 42,
    },
    {
        "incident_id": "INC-2044",
        "title": "Arbitrary Code Execution via Remote Shell Piping (curl | bash)",
        "category": "github-actions",
        "severity": "high",
        "service": "installer-script",
        "affected_services": ["installer-script"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 45,
        "summary": "Workflow step executed 'curl -fsSL https://external.com/install.sh | bash' without checksum verification.",
        "description": "A CI step piped an external installer directly into bash. A DNS hijacking event on the installer domain served an altered script that attempted to exfiltrate environment variables.",
        "root_cause": "Piping unverified remote HTTP content directly into shell interpreter.",
        "impact": "Execution of unverified remote code in CI build environment.",
        "resolution": "Downloaded script to file, verified SHA256 checksum, then executed script locally.",
        "timeline": "08:00 - Build executed; 08:12 - Egress anomaly alert; 08:45 - Checksum verification implemented.",
        "lessons_learned": "Always verify SHA256 checksums of remote scripts before executing them in CI/CD pipelines.",
        "preventive_controls": ["Checksum verification step", "Step-security egress filtering"],
        "tags": ["github-actions", "curl-pipe-sh", "remote-execution", "unverified-code"],
        "outcome": "Hotfix",
        "days_ago": 58,
    },

    # ── Secrets Leakage & Credential Management ──────────────────────────────
    {
        "incident_id": "INC-2050",
        "title": "Hardcoded Production Secret Key Leaked in Python Configuration",
        "category": "secrets",
        "severity": "critical",
        "service": "payment-api",
        "affected_services": ["payment-api", "stripe-gateway"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 55,
        "summary": "Stripe production live API key hardcoded in config.py committed to Git repository.",
        "description": "A developer committed `STRIPE_SECRET_KEY = 'sk_live_51...` in source code. GitHub secret scanning detected the key and alerted the security team.",
        "root_cause": "Hardcoded live API secret in source code file instead of environment variable / secrets manager.",
        "impact": "Potential unauthorized payment processing access, key revocation, and 55-minute payment API downtime.",
        "resolution": "Revoked Stripe live key, updated app to read secrets from AWS Secrets Manager, and purged git history.",
        "timeline": "11:30 - Commit pushed; 11:32 - GitHub Secret Scanning alert; 12:25 - Key rotated and app updated.",
        "lessons_learned": "Secrets must never be stored in source code. Use secret managers and git pre-commit hooks.",
        "preventive_controls": ["Git pre-commit TruffleHog scanner", "AWS Secrets Manager integration"],
        "tags": ["secrets", "hardcoded-credentials", "stripe-key", "git-leak", "api-key"],
        "outcome": "Security Incident",
        "days_ago": 7,
    },
    {
        "incident_id": "INC-2051",
        "title": "Hardcoded Database Password Committed in Service App Config",
        "category": "secrets",
        "severity": "high",
        "service": "user-service",
        "affected_services": ["user-service", "postgres-db"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 40,
        "summary": "PostgreSQL database production password committed in plaintext in settings.py.",
        "description": "A database connection string `postgres://admin:SuperSecretPass123@db.prod.internal/users` was hardcoded in application configuration.",
        "root_cause": "Plaintext database credentials committed to version control.",
        "impact": "Exposure of database master password to all repository read-access users.",
        "resolution": "Rotated database password, updated service to use environment variables, and redeployed.",
        "timeline": "14:00 - PR merged; 14:15 - Secret detected by internal audit; 14:40 - DB password changed.",
        "lessons_learned": "Store database credentials in environment variables or inject via Kubernetes Secrets.",
        "preventive_controls": ["GitGuardian secret detection", "Vault secrets injection"],
        "tags": ["secrets", "database-password", "hardcoded-credentials", "postgres"],
        "outcome": "Hotfix",
        "days_ago": 26,
    },

    # ── Authentication & Authorization Security ─────────────────────────────
    {
        "incident_id": "INC-2060",
        "title": "Authentication Middleware Removal Causes Unauthenticated API Access",
        "category": "authentication",
        "severity": "critical",
        "service": "user-profile-service",
        "affected_services": ["user-profile-service"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 50,
        "summary": "Refactoring removed `@require_auth` decorator from internal profile update routes, exposing user PII.",
        "description": "During a router refactoring, the `@require_auth` decorator was omitted from `/api/v1/users/profile`. Anyone could send a PATCH request with arbitrary user IDs to update profile data.",
        "root_cause": "Omission of authentication guard decorator during route refactoring.",
        "impact": "Unauthenticated modification of user profiles, PII tamper risk, and 50-minute emergency rollback.",
        "resolution": "Re-added `@require_auth` guard, added integration test verifying 401 Unauthorized for all unauthenticated requests.",
        "timeline": "10:00 - Refactored code deployed; 10:20 - Automated API security test flagged 200 OK on unauth endpoint; 10:50 - Rollback complete.",
        "lessons_learned": "Automated security tests must assert 401 Unauthorized across all non-public API endpoints.",
        "preventive_controls": ["Automated route authorization tests", "API schema security validation"],
        "tags": ["authentication", "auth-bypass", "missing-decorator", "pii-tamper", "api"],
        "outcome": "Rollback",
        "days_ago": 14,
    },
    {
        "incident_id": "INC-2061",
        "title": "JWT Signature Verification Disabled via 'alg: none' Misconfiguration",
        "category": "authentication",
        "severity": "critical",
        "service": "identity-provider",
        "affected_services": ["identity-provider", "all-microservices"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 80,
        "summary": "JWT library update accepted tokens with 'alg: none' header, permitting token forgery.",
        "description": "An update to the PyJWT validation options enabled `verify_signature = False` or accepted `alg: none`. Attackers forged arbitrary admin user tokens by crafting unsigned JWT payloads.",
        "root_cause": "Disabling JWT signature verification in PyJWT configuration options.",
        "impact": "Admin privilege escalation via forged JWT tokens, requiring emergency session invalidation.",
        "resolution": "Explicitly restricted allowed algorithms to `['RS256']`, enforced `verify_signature=True`, and revoked all active sessions.",
        "timeline": "13:00 - Identity service updated; 13:30 - Forged token activity detected; 14:20 - Patched JWT verifier deployed.",
        "lessons_learned": "Never allow 'none' algorithm in JWT verifiers. Explicitly specify allowed algorithms.",
        "preventive_controls": ["PyJWT algorithm whitelist", "Automated JWT vulnerability test suite"],
        "tags": ["authentication", "jwt", "signature-verification", "alg-none", "token-forgery"],
        "outcome": "Security Incident",
        "days_ago": 29,
    },
    {
        "incident_id": "INC-2062",
        "title": "Broken Object Level Authorization (BOLA / IDOR) in Order Endpoint",
        "category": "authorization",
        "severity": "high",
        "service": "order-management",
        "affected_services": ["order-management"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 65,
        "summary": "Endpoint GET /orders/{id} checked authentication but failed to verify that the order belonged to the requesting user.",
        "description": "Authenticated users could view any customer's order details by incrementing the order ID parameter, leading to mass PII data leakage.",
        "root_cause": "Missing tenant / object-level authorization check (order.user_id == current_user.id).",
        "impact": "Unauthorized access to customer order history and billing addresses.",
        "resolution": "Added ownership check `where user_id = current_user.id` to database query layer.",
        "timeline": "11:00 - Bug reported; 11:20 - Fix committed; 12:05 - Patched service deployed.",
        "lessons_learned": "Always enforce object-level authorization checks at the database/repository boundary.",
        "preventive_controls": ["BOLA integration tests", "Repository pattern tenant filter"],
        "tags": ["authorization", "bola", "idor", "tenant-isolation", "pii-leak"],
        "outcome": "Hotfix",
        "days_ago": 44,
    },

    # ── Database & Schema Migration Failures ─────────────────────────────────
    {
        "incident_id": "INC-2070",
        "title": "Unsafe Table Lock Outage during Schema Migration (ALTER TABLE)",
        "category": "database",
        "severity": "high",
        "service": "orders-db",
        "affected_services": ["orders-db", "checkout-service"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 75,
        "summary": "Running 'ALTER TABLE orders ADD COLUMN status VARCHAR NOT NULL' acquired AccessExclusiveLock, blocking all checkout writes.",
        "description": "An Alembic migration added a NOT NULL column without a default value to the 50-million-row orders table. PostgreSQL acquired an AccessExclusiveLock, causing query timeouts and cascade failure of checkout services.",
        "root_cause": "Non-concurrent DDL schema migration on high-traffic production table.",
        "impact": "75 minutes of complete checkout outage and transaction drop.",
        "resolution": "Cancelled migration, implemented 3-phase migration (Add nullable column -> Backfill -> Add NOT NULL constraint CONCURRENTLY).",
        "timeline": "15:00 - Migration started; 15:05 - Lock contention alert; 15:15 - Rollback initiated; 16:15 - System recovered.",
        "lessons_learned": "Large PostgreSQL schema changes must use multi-step zero-downtime migration patterns.",
        "preventive_controls": ["StrongMigrations linter for Rails/Alembic", "Migration timeout limits"],
        "tags": ["database", "migration", "table-lock", "outage", "postgres", "ddl"],
        "outcome": "Production Outage",
        "days_ago": 16,
    },
    {
        "incident_id": "INC-2071",
        "title": "PostgreSQL Database Public Endpoint Exposed to Public Internet",
        "category": "database",
        "severity": "critical",
        "service": "analytics-postgres",
        "affected_services": ["analytics-postgres", "aws-rds"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 55,
        "summary": "PostgreSQL RDS instance security group opened port 5432 to 0.0.0.0/0.",
        "description": "An emergency debugging Terraform change allowed inbound port 5432 from 0.0.0.0/0. Automated external scanners attempted brute-force password guessing against the database.",
        "root_cause": "Public security group rule allowing 0.0.0.0/0 PostgreSQL access.",
        "impact": "External network exposure of production database port, requiring security group cleanup.",
        "resolution": "Removed 0.0.0.0/0 ingress rule, restricted access to VPC CIDR, and rotated DB credentials.",
        "timeline": "09:00 - SG change applied; 09:15 - Brute force alerts; 09:55 - Network access restricted.",
        "lessons_learned": "Never open database ports to public internet. Enforce VPC-only security group rules.",
        "preventive_controls": ["AWS Config rds-instance-public-access-check", "Checkov CKV_AWS_24"],
        "tags": ["database", "postgres", "open-port", "public-access", "security-group"],
        "outcome": "Security Incident",
        "days_ago": 36,
    },

    # ── Networking, Web & Infrastructure ────────────────────────────────────
    {
        "incident_id": "INC-2080",
        "title": "SSRF Exposure to AWS EC2 Instance Metadata Service (IMDSv1)",
        "category": "web-security",
        "severity": "critical",
        "service": "pdf-generator",
        "affected_services": ["pdf-generator", "aws-iam-role"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 85,
        "summary": "SSRF in PDF generator service allowed fetching http://169.254.169.254/latest/meta-data/iam/security-credentials/.",
        "description": "A PDF rendering service allowed users to specify image URLs. An attacker specified `http://169.254.169.254/latest/meta-data/...` and exfiltrated the EC2 node's IAM role temporary credentials.",
        "root_cause": "SSRF vulnerability combined with unconstrained IMDSv1 access.",
        "impact": "Exfiltration of EC2 IAM role credentials and potential AWS API access.",
        "resolution": "Enforced AWS IMDSv2 (http_tokens=required, hop_limit=1), added URL validation whitelist to PDF generator.",
        "timeline": "14:00 - SSRF exploited; 14:25 - GuardDuty IMDS anomaly alert; 15:25 - IMDSv2 enforced across node pool.",
        "lessons_learned": "Require IMDSv2 (token-based) across all AWS EC2 instances to neutralize SSRF IMDS theft.",
        "preventive_controls": ["AWS IMDSv2 enforcement", "Egress network policy filtering", "URL whitelist validation"],
        "tags": ["web-security", "ssrf", "imds", "aws-iam", "credential-theft"],
        "outcome": "Security Incident",
        "days_ago": 11,
    },
    {
        "incident_id": "INC-2081",
        "title": "SQL Injection in User Login Handler Bypassing Authentication",
        "category": "web-security",
        "severity": "critical",
        "service": "legacy-auth-api",
        "affected_services": ["legacy-auth-api", "users-db"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 45,
        "summary": "String concatenation in raw SQL query allowed authentication bypass via 'admin' -- tautology.",
        "description": "A legacy login handler executed `f\"SELECT * FROM users WHERE email='{username}' AND password='{password}'\"`. Entering `admin' --` logged the user in as administrator without a valid password.",
        "root_cause": "Unsanitized user input string formatting in raw SQL query.",
        "impact": "Unauthorized admin account takeover, 45-minute emergency deployment rollback.",
        "resolution": "Replaced raw SQL string interpolation with parameterized SQL query bindings (`cursor.execute(sql, (user, pass))`).",
        "timeline": "09:00 - Exploit detected in WAF logs; 09:15 - Rollback initiated; 09:45 - Parameterized query fix deployed.",
        "lessons_learned": "Never concatenate user input into SQL queries. Always use parameterized query placeholders.",
        "preventive_controls": ["Bandit B608 SQL injection linter", "Parameterized query enforcement"],
        "tags": ["web-security", "sqli", "auth-bypass", "parameterized-queries", "sql"],
        "outcome": "Rollback",
        "days_ago": 24,
    },
    {
        "incident_id": "INC-2082",
        "title": "Remote Code Execution via Python YAML SafeLoad Omission",
        "category": "web-security",
        "severity": "critical",
        "service": "config-parser-service",
        "affected_services": ["config-parser-service"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 60,
        "summary": "Using yaml.load(input, Loader=yaml.Loader) enabled arbitrary Python object instantiation and RCE.",
        "description": "A service accepted user-uploaded YAML configs and parsed them using unsafe `yaml.load()`. An attacker uploaded a payload containing `!!python/object/apply:subprocess.Popen` to execute shell commands.",
        "root_cause": "Use of unsafe PyYAML `yaml.load()` instead of `yaml.safe_load()`.",
        "impact": "Remote code execution in container context, emergency service isolation.",
        "resolution": "Replaced all `yaml.load()` calls with `yaml.safe_load()`, updated SAST rules.",
        "timeline": "16:00 - Malicious YAML uploaded; 16:10 - Outbound connection alert; 17:00 - `yaml.safe_load` patch deployed.",
        "lessons_learned": "Always use safe deserializers (`yaml.safe_load()`, `json.loads()`) when parsing untrusted data.",
        "preventive_controls": ["Bandit B506 yaml_load rule", "Safe deserialization linter"],
        "tags": ["web-security", "rce", "deserialization", "pyyaml", "python"],
        "outcome": "Security Incident",
        "days_ago": 35,
    },
    {
        "incident_id": "INC-2083",
        "title": "Dependency Supply Chain Attack via Malicious PyPI Package",
        "category": "supply-chain",
        "severity": "critical",
        "service": "analytics-worker",
        "affected_services": ["analytics-worker"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 90,
        "summary": "Typosquatting package 'reqeusts-helpers' in requirements.txt executed malicious setup.py payload.",
        "description": "A typo in requirements.txt introduced `reqeusts-helpers` which contained malicious code in `setup.py` that exfiltrated environment variables during `pip install`.",
        "root_cause": "Typosquatting dependency added without code review verification.",
        "impact": "Exfiltration of CI build environment variables, requiring key rotation and dependency cleanup.",
        "resolution": "Removed typosquatted package, added private PyPI proxy (Artifactory) with package whitelist.",
        "timeline": "10:00 - Dependency added; 10:15 - CI build ran setup.py payload; 11:45 - Package revoked and keys rotated.",
        "lessons_learned": "Use private repository mirrors and lockfile hash verification (`pip install --require-hashes`).",
        "preventive_controls": ["Pipfile.lock / poetry.lock hash verification", "Private package mirror whitelist"],
        "tags": ["supply-chain", "dependency", "typosquatting", "pypi", "malicious-package"],
        "outcome": "Security Incident",
        "days_ago": 50,
    },
    {
        "incident_id": "INC-2084",
        "title": "TLS Certificate Verification Bypass in Internal Microservice Client",
        "category": "networking",
        "severity": "high",
        "service": "payment-gateway-client",
        "affected_services": ["payment-gateway-client", "payment-service"],
        "environment": "production",
        "rollback": True,
        "duration_minutes": 40,
        "summary": "Setting verify=False in Python requests client disabled TLS certificate validation.",
        "description": "To bypass staging self-signed cert errors, a developer added `verify=False` to HTTP requests client code. The change leaked into production, exposing payment traffic to MITM attacks.",
        "root_cause": "Disabling TLS certificate verification (`verify=False`) in production HTTP client.",
        "impact": "Potential Man-in-the-Middle exposure of payment payloads over internal network.",
        "resolution": "Re-enabled `verify=True`, added internal CA certificates to container trust store, and pushed hotfix.",
        "timeline": "13:00 - Deploy completed; 13:10 - SAST audit alert; 13:40 - Hotfix deployed.",
        "lessons_learned": "Never disable TLS verification in code. Install custom CAs into the system trust store instead.",
        "preventive_controls": ["Bandit B501 requests_verify_false rule", "TLS enforcement policy"],
        "tags": ["networking", "tls", "mitm", "certificate-validation", "requests"],
        "outcome": "Hotfix",
        "days_ago": 56,
    },
    {
        "incident_id": "INC-2085",
        "title": "Redis Cache Unauthenticated Access and Memory Exhaustion",
        "category": "redis",
        "severity": "high",
        "service": "session-cache",
        "affected_services": ["session-cache", "redis-cluster"],
        "environment": "production",
        "rollback": False,
        "duration_minutes": 65,
        "summary": "Redis cluster deployed without requirepass authentication was flooded with unauthenticated keys.",
        "description": "A new Redis cluster was deployed with `protected-mode no` and empty `requirepass`. An internal service leak flooded Redis with unauthenticated key writes, causing OOM evictions of user sessions.",
        "root_cause": "Omission of authentication (`requirepass`) and protected-mode on Redis cluster.",
        "impact": "User session cache evictions causing mass logout for 10,000 active users.",
        "resolution": "Enabled `requirepass`, set `protected-mode yes`, and configured maxmemory maxmemory-policy volatile-lru.",
        "timeline": "08:00 - Redis deployed; 08:30 - Mass session invalidation; 09:35 - Auth enabled and memory policy set.",
        "lessons_learned": "Redis instances must always enforce strong authentication and memory eviction policies.",
        "preventive_controls": ["Redis requirepass requirement", "Terraform Redis security module"],
        "tags": ["redis", "unauthenticated", "cache-eviction", "outage", "session"],
        "outcome": "Production Outage",
        "days_ago": 64,
    },
]

# Generate additional variations if needed to ensure 55-65 high-quality incidents
def build_curated_dataset(minimum: int = 50, maximum: int = 80) -> list[SeedIncident]:
    now = datetime.now(timezone.utc)
    incidents: list[SeedIncident] = []

    # Process explicit curated specs
    for idx, spec in enumerate(CURATED_INCIDENT_SPECS):
        days_ago = spec.get("days_ago", idx * 2 + 1)
        created_at = (now - timedelta(days=days_ago, hours=idx % 12)).isoformat()

        incidents.append(
            SeedIncident(
                incident_id=spec["incident_id"],
                title=spec["title"],
                description=spec["description"],
                severity=spec["severity"],
                outcome=spec.get("outcome", "Hotfix"),
                rollback=spec.get("rollback", False),
                duration_minutes=spec.get("duration_minutes", 45),
                environment=spec.get("environment", "production"),
                service=spec.get("service", "core-service"),
                root_cause=spec.get("root_cause", ""),
                tags=spec.get("tags", []),
                created_at=created_at,
                summary=spec.get("summary", spec["description"]),
                impact=spec.get("impact", ""),
                resolution=spec.get("resolution", ""),
                affected_services=spec.get("affected_services", [spec.get("service", "core-service")]),
                timeline=spec.get("timeline", ""),
                lessons_learned=spec.get("lessons_learned", ""),
                preventive_controls=spec.get("preventive_controls", []),
                metadata={
                    "category": spec.get("category", "general"),
                    "curated": True,
                },
            )
        )

    # Pad with realistic variants if necessary to maintain minimum target (50-60)
    if len(incidents) < minimum:
        extra_count = minimum - len(incidents)
        for i in range(extra_count):
            inc_num = 2100 + i
            created_at = (now - timedelta(days=70 + i * 2)).isoformat()
            incidents.append(
                SeedIncident(
                    incident_id=f"INC-{inc_num}",
                    title=f"Infrastructure Security Variance {i+1}",
                    description=f"Automated control deviation detected in security audit {i+1}.",
                    severity="medium" if i % 2 == 0 else "low",
                    outcome="Manual Intervention",
                    rollback=False,
                    duration_minutes=30,
                    environment="staging",
                    service="infra-service",
                    root_cause="Configuration drift during maintenance window.",
                    tags=["infrastructure", "audit", "compliance"],
                    created_at=created_at,
                    summary=f"Automated control deviation detected in audit {i+1}.",
                    impact="Minor staging configuration inconsistency.",
                    resolution="Applied automated Terraform state sync to resolve drift.",
                    affected_services=["infra-service"],
                    timeline="Audit flagged inconsistency; Terraform state refreshed.",
                    lessons_learned="Automate Terraform drift detection on a scheduled cron.",
                    preventive_controls=["Terraform drift detection cron"],
                    metadata={"curated": True},
                )
            )

    if len(incidents) > maximum:
        incidents = incidents[:maximum]

    return incidents
